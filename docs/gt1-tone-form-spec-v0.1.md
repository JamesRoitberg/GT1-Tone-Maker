# Especificacao GT1_TONE_FORM_V0_1

Este arquivo existe apenas como uma nota de compatibilidade documental.

A especificacao oficial e atual do formulario `GT1_TONE_FORM_V0_1` fica em:

`docs/gt1-tone-form-v0.1.md`

Use esse documento como fonte de verdade para:

- campos obrigatorios;
- enums permitidos;
- ranges numericos;
- regras de parser;
- regras de validacao;
- exemplo completo do formulario.

Nao mantenha duas especificacoes paralelas para o mesmo contrato. Se o contrato mudar, atualize `docs/gt1-tone-form-v0.1.md`, o contrato em codigo e os testes relacionados.
